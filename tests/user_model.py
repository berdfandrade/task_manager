import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from sqlalchemy.exc import IntegrityError
from models.user import User, Base  # Substitua 'your_module' pelo nome do seu módulo

# TODO : Continuar aqui...